<?xml version="1.0" encoding="utf-8"?>
<!-- Layout for a single transaction item in the RecyclerView. -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="?attr/colorPrimaryVariant"> <!-- Uses theme color for background -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Category Icon -->
        <ImageView
            android:id="@+id/category_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="16dp"
            android:src="@drawable/ic_category_placeholder"
            android:contentDescription="@string/category_label"
            app:tint="?attr/colorOnPrimary" /> <!-- Tint with onPrimary color -->

        <!-- Transaction Details (Description, Category, Date) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/transaction_description_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnPrimary"
                tools:text="Coffee Shop Purchase" />

            <TextView
                android:id="@+id/transaction_category_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textColor="?attr/colorOnPrimary"
                android:alpha="0.8"
                tools:text="Food" />

            <TextView
                android:id="@+id/transaction_date_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="?attr/colorOnPrimary"
                android:alpha="0.7"
                tools:text="28/06/2025" />
        </LinearLayout>

        <!-- Amount and Delete Button -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginStart="16dp">

            <TextView
                android:id="@+id/transaction_amount_text_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="৳ -150.00"
                android:layout_marginEnd="8dp"/>

            <ImageButton
                android:id="@+id/delete_transaction_button"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_delete"
                android:contentDescription="@string/delete_transaction_description"
                app:tint="?attr/colorSecondaryVariant"/> <!-- A slightly different tint for action buttons -->
        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
