<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state (concave effect for red button) -->
    <item android:state_pressed="true">
        <layer-list>
            <item android:top="2dp" android:left="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="12dp"/>
                    <solid android:color="@color/red_expense_dark_shadow"/> <!-- Darker red inner shadow -->
                </shape>
            </item>
            <item android:bottom="2dp" android:right="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="12dp"/>
                    <solid android:color="@color/red_expense_light_shadow"/> <!-- Lighter red inner highlight -->
                </shape>
            </item>
            <item android:left="1dp" android:top="1dp" android:right="1dp" android:bottom="1dp">
                <shape android:shape="rectangle">
                    <corners android:radius="12dp"/>
                    <solid android:color="@color/red_expense"/> <!-- Main red button face -->
                </shape>
            </item>
        </layer-list>
    </item>
    <!-- Default state (convex effect for red button) -->
    <item>
        <layer-list>
            <item android:bottom="2dp" android:right="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="12dp"/>
                    <solid android:color="@color/red_expense_dark_shadow"/> <!-- Darker red outer shadow -->
                </shape>
            </item>
            <item android:top="2dp" android:left="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="12dp"/>
                    <solid android:color="@color/red_expense_light_shadow"/> <!-- Lighter red outer highlight -->
                </shape>
            </item>
            <item android:left="0dp" android:top="0dp" android:right="0dp" android:bottom="0dp">
                <shape android:shape="rectangle">
                    <corners android:radius="12dp"/>
                    <solid android:color="@color/red_expense"/> <!-- Main red button face -->
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
